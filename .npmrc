# Override user-level scope registry (avoid GitHub Packages which needs auth).
# @khal-os/* packages are public on npmjs.org — no token needed for reads.
# Committed intentionally; contains NO secrets.
@khal-os:registry=https://registry.npmjs.org/
